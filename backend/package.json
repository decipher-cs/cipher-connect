{
  "name": "server-auth",
  "version": "0.0.0",
  "description": "testing auth0",
  "main": "./dist/server.js",
  "license": "GNU GENERAL PUBLIC LICENSE",
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS='--no-warnings --loader ts-node/esm' nodemon --watch 'src/**/*' -e ts,tsx --exec ts-node ./src/**",
    "build": "tsc",
    "run": "tsc && node ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.6.1",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "dotenv": "^16.0.3",
    "express-openid-connect": "^2.14.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1"
  },
  "nodemonConfig": {
    "events": {
      "start": "clear && echo '--------------------------'"
    }
  }
}
